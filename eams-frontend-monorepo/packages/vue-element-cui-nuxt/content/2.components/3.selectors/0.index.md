---
title: 选择器
description: select、enum、dict 与选择容器能力
---

# 选择器

这个体系页把旧的 `select`、`select-dict`、`select-enum`、`radio-dict`、`radio-enum`、`checkbox-enum`、`autocomplete` 与 `select-box` 整理到一起，减少导航平铺。

<FeatureList
	:items='[
		{ "title": "CuiSelect", "description": "处理普通单选、多选和过滤场景。" },
		{ "title": "Dict / Enum 选择组件", "description": "当选项来自字典或枚举时，优先使用专用组件，而不是在页面层手写转换。" },
		{ "title": "CuiSelectBox", "description": "当普通下拉不足以承载交互时，再引入选择容器。" }
	]'
/>

- [总览页](/components/selectors/overview)
